# Portfolio
This is an updated version of Frontend Developer portfolio, that is based on task from Intro to JS Course @ RS School (aka We are Community). The new version is using React library and supports wider functionality. 
### Run 
`npm install`
`npm run dev`

